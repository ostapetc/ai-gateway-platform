apiVersion: apps/v1
kind: Deployment
metadata:
  name: users
  namespace: {{ .Release.Namespace }}
  labels:
    app: users
    {{- include "ai-gateway.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.apps.users.replicas }}
  selector:
    matchLabels:
      app: users
  template:
    metadata:
      labels:
        app: users
    spec:
      containers:
        - name: users
          image: {{ .Values.registry }}/users:{{ .Values.tag }}
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.apps.users.ports.http }}
            - name: grpc
              containerPort: {{ .Values.apps.users.ports.grpc }}
          env:
            - name: ETCD_ADDRESS
              value: "etcd.{{ .Release.Namespace }}:2379"
          readinessProbe:
            tcpSocket:
              port: {{ .Values.apps.users.ports.http }}
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: {{ .Values.apps.users.ports.http }}
            initialDelaySeconds: 10
            periodSeconds: 30
          resources:
            {{- toYaml .Values.apps.users.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: users
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: users
  ports:
    - name: http
      port: {{ .Values.apps.users.ports.http }}
      targetPort: {{ .Values.apps.users.ports.http }}
    - name: grpc
      port: {{ .Values.apps.users.ports.grpc }}
      targetPort: {{ .Values.apps.users.ports.grpc }}
