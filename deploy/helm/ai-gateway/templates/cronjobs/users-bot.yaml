{{- if .Values.cronjobs.users.enabled }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: users-bot-cronjob
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ai-gateway.labels" . | nindent 4 }}
spec:
  schedule: {{ .Values.cronjobs.users.schedule | quote }}
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: users-bot
              image: {{ .Values.registry }}/users-bot-cronjob:{{ .Values.tag }}
              imagePullPolicy: {{ .Values.imagePullPolicy }}
              args:
                - createusers
              env:
                - name: ETCD_ADDRESS
                  value: "etcd.{{ .Release.Namespace }}:2379"
          restartPolicy: OnFailure
{{- end }}
