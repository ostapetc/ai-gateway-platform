apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-datasources
  namespace: ai-gateway
data:
  datasources.yaml: |
    apiVersion: 1
    datasources:
      - name: Prometheus
        type: prometheus
        uid: prometheus
        url: http://prometheus:9090
        isDefault: true
        editable: false
